{{- if .Values.echo-server.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "echo_server.fullname" . }}
  labels:
    {{- include "echo_server.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "echo_server.fullname" . }}
  minReplicas: {{ .Values.echo-server.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.echo-server.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.echo-server.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.echo-server.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.echo-server.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.echo-server.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
